# Stock Screener 2.0 — Python dependencies
# Install with: pip install -r requirements.txt

# ── Core data ─────────────────────────────────────────────────────────────────
yfinance>=0.2.40          # Market data (OHLCV, fundamentals, pre/after-hours)
pandas>=2.0               # Data manipulation
numpy>=1.24               # Numerical computing
pandas-ta>=0.3.14b        # Technical indicators (RSI, MACD, BB, ATR etc.)
requests>=2.31            # HTTP client (NewsAPI, time API)

# ── Machine learning ──────────────────────────────────────────────────────────
scikit-learn>=1.3         # Preprocessing, model utilities
xgboost>=2.0              # XGBoost price prediction models

# ── NLP / Sentiment ───────────────────────────────────────────────────────────
transformers>=4.30        # HuggingFace — FinBERT sentiment analysis
torch>=2.0                # PyTorch backend for transformers

# ── Dashboard & visualization ─────────────────────────────────────────────────
streamlit>=1.35           # Web dashboard framework
plotly>=5.22              # Interactive charts

# ── Utilities ─────────────────────────────────────────────────────────────────
holidays>=0.46            # Market holiday detection (used in backtester)

# ── Optional ─────────────────────────────────────────────────────────────────
# praw>=7.7               # Reddit sentiment (not currently active)
# fredapi>=0.5            # FRED macro data (currently via requests directly)
